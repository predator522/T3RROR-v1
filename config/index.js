import configData from "./config.json" assert { type: "json" };
import messagesData from "./messages.json" assert { type: "json" };

export const config = configData;
export const msgs = messagesData;
